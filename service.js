// service.js - GANZ EINFACHE VERSION
async function extractDetails(url) {
    return [];
}

async function searchResults(keyword) {
    return [];
}

async function extractEpisodes(url) {
    return [];
}

async function extractStreamUrl(url) {
    return null;
}

module.exports = {
    extractDetails,
    searchResults,
    extractEpisodes,
    extractStreamUrl
};
